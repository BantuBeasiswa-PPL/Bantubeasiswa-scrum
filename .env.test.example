# ============================================================
# TEMPLATE kredensial DATABASE TEST (project Supabase: db_automated)
# Dipakai saat menjalankan test real-DB: `npm run test:e2e:db`
#
# CARA PAKAI (tiap anggota tim):
#   1. Salin file ini menjadi `.env.test`   (cp .env.test.example .env.test)
#   2. Isi 2 kunci di bawah dari Supabase project db_automated:
#        Settings -> API -> "anon public" dan "service_role secret"
#   3. URL & JWT_SECRET sudah benar, biarkan apa adanya.
#
# CATATAN: `.env.test` (berisi kunci asli) DI-GITIGNORE -> jangan pernah di-commit.
#          Minta kunci asli ke pemilik project lewat jalur aman (bukan di repo).
# ============================================================

NEXT_PUBLIC_SUPABASE_URL=https://ncezlpqllmrfdqvtbpoq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ISI_ANON_KEY_DB_AUTOMATED
SUPABASE_SERVICE_ROLE_KEY=ISI_SERVICE_ROLE_KEY_DB_AUTOMATED

# Harus sama dengan secret di e2e/helpers.js (untuk login JWT test)
JWT_SECRET=bantubeasiswa_secret_key_ppl_2026_ganti_ini
